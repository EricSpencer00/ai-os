# ⚡ Quick Reference Card

## Current Status: ✅ FULLY OPERATIONAL

### Access the System

**Web Browser:**
```
http://localhost:6080/vnc.html
Password: auraos123
```

### Essential Commands

```bash
# Start everything
./auraos.sh install         # One-time setup
./auraos.sh vm-setup        # Creates VM
./auraos.sh forward start   # Enable port forwarding
./auraos.sh health          # Verify all systems

# Monitor and manage
./auraos.sh status          # Quick status
./auraos.sh health          # Detailed health check
./auraos.sh forward status  # Port forwarding status

# Run AI automation
./auraos.sh automate "open Firefox"

# Logs and diagnostics
./auraos.sh logs            # View daemon logs
multipass list              # List VMs
```

### Port Mapping

| URL/Address | Purpose |
|---|---|
| http://localhost:6080/vnc.html | Web desktop |
| localhost:5901 | Direct VNC |
| localhost:8765 | AI daemon |
| 192.168.2.11 | VM IP (direct) |

### VM Access

```bash
# SSH into VM
multipass shell auraos-multipass

# Run command in VM
multipass exec auraos-multipass -- systemctl status auraos-x11vnc.service
```

### Troubleshooting

```bash
# Restart services
./auraos.sh gui-reset

# Check service status
./auraos.sh health

# View detailed logs
./auraos.sh logs

# Recreate VM
./auraos.sh vm-setup  # Will prompt if VM exists
```

### What's Installed

In VM:
- ✅ Ubuntu 22.04 LTS
- ✅ GNOME Desktop
- ✅ x11vnc (VNC server)
- ✅ noVNC (web interface)
- ✅ AuraOS Terminal
- ✅ AI Daemon
- ✅ Ollama models

On macOS:
- ✅ Homebrew
- ✅ Multipass
- ✅ Ollama
- ✅ Python 3
- ✅ Port forwarders

### Performance

- VM startup: ~15s
- Desktop load: ~2-3s
- Web connection: instant
- VNC latency: ~20-30ms

### File Locations

- Config: `/Users/eric/GitHub/ai-os/auraos_daemon/config.json`
- Logs: `/Users/eric/GitHub/ai-os/logs/`
- Scripts: `/Users/eric/GitHub/ai-os/`
- VM resources: `/Users/eric/GitHub/ai-os/vm_resources/`

---

## Setup One-Liner

```bash
./auraos.sh install && ./auraos.sh vm-setup && ./auraos.sh forward start && echo "Ready! Open http://localhost:6080/vnc.html"
```

---

## Status Check

All systems operational:
- ✅ x11vnc service running
- ✅ noVNC proxy running
- ✅ Port forwarding active
- ✅ Web interface responsive
- ✅ VM fully configured

**You're good to go!** 🚀
